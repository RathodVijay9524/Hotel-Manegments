spring.application.name=User-Master
server.port=9092
#spring.security.user.name=user
#spring.security.user.password=password

spring.datasource.url=jdbc:mysql://localhost:3306/user-master
spring.datasource.username=root
spring.datasource.password=root

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
#spring.jpa.properties.hibernate.format_sql=true

# Performance optimizations
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true

logging.level.com.vijay.User_Master.service.impl=INFO
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN

app.jwt-secret=daf66e01593f61a15b857cf433aae03a005812b31234e149036bcc8dee755dbb
app-jwt-expiration-milliseconds=604800000

user.profile.image.path=images/users/
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB


logging.level.org.springframework.security=WARN
logging.level.com.vijay.User_Master.exception=ERROR
logging.level.org.springframework=WARN
logging.level.org.springframework.boot=WARN

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=omvijay44@gmail.com
spring.mail.password=
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- Springdoc OpenAPI configuration ---
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
# Limit scanning to controllers to avoid 500s from scanning entities or other packages
springdoc.packages-to-scan=com.vijay.User_Master.controller
# Only document API paths under /api/**
springdoc.paths-to-match=/api/**

# Trust proxy headers from Cloudflare Tunnel so app builds correct absolute URLs and HTTPS scheme
server.forward-headers-strategy=framework

# Chat Service Configuration
# Use localhost for internal communication between services
chat.service.base-url=http://localhost:8080
chat.service.timeout=30

# Connection timeout settings
server.tomcat.connection-timeout=20000
server.tomcat.keep-alive-timeout=20000

# Reduce logging noise from client disconnections
logging.level.org.apache.catalina.connector.CoyoteAdapter=WARN
logging.level.org.apache.coyote.http11.Http11Processor=WARN

# Logging for debugging chat integration
logging.level.com.vijay.User_Master.service.ChatIntegrationService=WARN
logging.level.org.springframework.web.reactive.function.client=WARN

# ===== PERFORMANCE OPTIMIZATIONS =====

# Enable lazy initialization for faster startup (controllers will load on first request)
spring.main.lazy-initialization=true

# Reduce context refresh overhead
spring.main.register-shutdown-hook=false

# Skip condition evaluation on startup
spring.cloud.refresh.enabled=false

# Reduce Tomcat startup time
server.tomcat.max-threads=50
server.tomcat.min-spare-threads=10

# HikariCP Connection Pool optimization
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# Disable unnecessary features
spring.jpa.open-in-view=false
spring.main.banner-mode=off
spring.jpa.properties.hibernate.generate_statistics=false

# Cache prepared statements
spring.datasource.hikari.data-source-properties.cachePrepStmts=true
spring.datasource.hikari.data-source-properties.prepStmtCacheSize=250
spring.datasource.hikari.data-source-properties.prepStmtCacheSqlLimit=2048
spring.datasource.hikari.data-source-properties.useServerPrepStmts=true

# Reduce Hibernate schema validation time
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Disable unnecessary Spring Boot features
spring.jmx.enabled=false
management.endpoints.enabled-by-default=false
